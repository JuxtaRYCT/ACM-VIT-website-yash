---
// Gallery.astro
import { getAssetUrl } from "../utils/cdn";
import ProofGallery from "../components/overlays/ProofGallery.astro";
---

<section
  class="gallery-section relative w-full pt-16 pb-0 sm:pt-24 sm:pb-0 overflow-hidden"
>
  <div class="gallery-frame relative w-full overflow-hidden">
    <img
      src={getAssetUrl("/gallery/gallery-hero.png")}
      alt="ACM-VIT Gallery"
      class="gallery-image w-full object-cover"
      loading="lazy"
      decoding="async"
    />
    <!-- Button overlaid on image, below the GALLERY text (~45% from top) -->
    <div class="absolute left-0 right-0 top-[35%] z-10 flex justify-center px-4">
      <div
        onclick="window.openProofGallery()"
        class="w-[484px] max-w-[90vw] h-12 bg-[#F95F4A] rounded-[41px] outline outline-1 outline-offset-[-1px] outline-yellow-50
           flex items-center justify-center hover:scale-105 transition-transform duration-200 cursor-pointer"
        role="button"
        tabindex="0"
      >
        <span
          class="text-white text-base font-light font-['PolySans_Trial'] uppercase"
        >
          Proof We Actually Do Stuff - View Photos!
        </span>
      </div>
    </div>
  </div>
  <ProofGallery />
</section>

<script>
  // Ensure the function is available (if ProofGallery script hasn't run yet, though it should have)
  // Actually, Astro scripts module hoist, so it might be fine, but let's be safe.
</script>

<style>
  .gallery-section {
    font-family: "PolySans Trial", "PolySans", system-ui, sans-serif;
    -webkit-user-select: none;
    user-select: none;
    -webkit-touch-callout: none;
  }

  .gallery-section * {
    -webkit-user-select: none;
    user-select: none;
  }

  .gallery-section img {
    -webkit-user-drag: none;
  }

  .gallery-frame::after {
    content: "";
    position: absolute;
    inset: 0;
    background: linear-gradient(
      180deg,
      rgba(3, 3, 3, 0) 40%,
      rgba(3, 3, 3, 0.55) 100%
    );
    pointer-events: none;
  }

  .gallery-image {
    filter: grayscale(1);
    transition: filter 0.3s ease;
  }

  .gallery-frame:hover .gallery-image {
    filter: grayscale(0.2);
  }
</style>
